get:
  tags: [oauth]
  summary: JSON Web Key Set (JWKS)
  description: Public keys for JWT validation (RS256). Configure Spring Security Resource Server with jwk-set-uri to this endpoint.
  responses:
    '200':
      description: JWKS
      content:
        application/json:
          schema:
            $ref: ../model/JwksResponse.yml