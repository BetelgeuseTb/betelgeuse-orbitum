type: object
required:
  - client_id
  - created_at
properties:
  client_id:
    type: string
  client_secret:
    type: string
    description: Only returned for confidential clients
  created_at:
    type: string
    format: date-time